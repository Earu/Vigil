{
    "name": "password-manager-extension",
    "version": "1.0.0",
    "description": "Password Manager Browser Extension",
    "type": "module",
    "scripts": {
        "build": "node build.js",
        "watch": "node build.js --watch",
        "dev:firefox": "npm run build && web-ext run",
        "build:firefox": "npm run build && web-ext build",
        "build:chrome": "npm run build"
    },
    "devDependencies": {
        "@types/chrome": "^0.0.254",
        "@types/firefox-webext-browser": "^111.0.1",
        "@types/uuid": "^10.0.0",
        "esbuild": "^0.20.1",
        "typescript": "^5.0.0",
        "web-ext": "^7.8.0"
    }
}
