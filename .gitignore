# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Electron
dist-electron

# Native modules
native/**/build/
native/**/-p/
native/**/prebuilds/
native/**/*.node
native/**/CMakeFiles/
native/**/CMakeCache.txt
native/**/cmake_install.cmake
*.vcxproj
*.vcxproj.filters
*.vcxproj.user
*.lib
*.exp
*.pdb

# Argon2
public
# TypeScript incremental build state
*.tsbuildinfo

# native addon build output (compiled per-platform, never committed)
electron/native/*/build/

# Signing material. The provisioning profile embeds the team name and the
# certificate is private key material; neither belongs in a public repo.
# CI restores the profile from the MAC_PROVISION_PROFILE secret.
build/vigil.provisionprofile
*.provisionprofile
*.p12
*.cer
*.p8

# scripts/fuzz-native.mjs: the built target, its corpus copy and any
# reproducing input it finds
native-fuzz-out/

# The hosted share page: derived from src/services/sharePage.ts by
# scripts/build-share-page.mjs and published by CI, never committed
docs/
_site/
